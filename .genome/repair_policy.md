# Repair policy (optional)

- **max_retries:** Number of retry attempts after the first run fails (0 = no retry).
- **delay_ms:** Delay in milliseconds between attempts (0 = no delay).

max_retries: 2
delay_ms: 0
