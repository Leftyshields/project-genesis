# Task closed: EPH-20250314-0010

**Title:** Implement one full path end-to-end (Build → tissue → cell → molecule) from backlog (Story 9)

**Closed:** 2025-03-15

**Outcome:** Delivered. `lib/run.js` (runPath(options?), resolveMolecule(roleId)), `lib/run.test.js` (10 tests), `scripts/run-path.js`, `lib/README.md` (runPath section and roleId → implementation convention), `package.json` (npm test includes run tests). One full path runs: load genome → decompose → overlay → walk to leaf molecule → resolve `.molecules/lib/<roleId>.js` → invoke molecule (read_file) → set overlay; returns `{ root, overlay, result? }`. Script: `node scripts/run-path.js [path]` prints ok/failed and optional preview. Code review applied: resolveMolecule returns only `{ fn }`; roleId guard for missing/invalid. QA checklist (Story 9 section) and 40 tests (including script and edge-case coverage) passed. GitHub #9 closed.

**No further action required for this task.**
