# Task closed: EPH-20250314-0012

**Title:** Add Defense organ / guardrails enforcement from backlog (Story 11)

**Closed:** 2025-03-15

**Outcome:** Delivered. Guardrails run at the start of runPath (after loadGenome): `checkGuardrails(genome, request)` enforces path allowlist; violations are blocked and audited to `.logs/guardrails.log`. Optional `.genome/guardrails.md` (`allowed_path_prefix: .genome/,.molecules/`); default allowlist `['.genome/']`. Delivered: `lib/guardrails.js` (checkGuardrails, auditViolation, parseGuardrailsPolicy), `lib/loadGenome.js` (optional guardrails read), `lib/run.js` (guard check and blocked return shape), `lib/guardrails.test.js`, `lib/run.test.js` updates, `.genome/guardrails.md`, `lib/README.md` (Guardrails section), `package.json` (guardrails tests). Return when blocked: `blocked: true`, `violationReason`, overlay molecule failed. Code review: JSDoc updates. QA: manual checklist and auto-tests (61 pass). Closes #11.

**No further action required for this task.**
