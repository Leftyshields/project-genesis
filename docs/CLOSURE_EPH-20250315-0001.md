# Task closed: EPH-20250315-0001

**Title:** Instantiation workflow: script to copy Project Genesis into new project directory

**Closed:** 2025-03-15

**Outcome:** Delivered. (1) `instantiate.sh` at repo root: copy-all-except (`.git`, `.logs/`, `.ai/`, `.tmp-*`, `node_modules`), creates empty `.ai/context/`, optional `npm test` verification; `--force` and `--no-verify`; validates source looks like PG, target outside source, target empty unless `--force`. (2) README: new intro ("You're the Creator. One prompt. One app."), Quick start and Instantiation sections before nerdy details; build-step fact check (Cursor runs build when you confirm); typo fix. (3) `scripts/instantiate.test.js`: asserts critical paths in copy (package.json, .genome, .cursor/commands, docs, lib, scripts/run-path.js, .ai/context); included in `npm test`. (4) `package.json`: test script includes instantiate.test.js. (5) Design decisions (`.ai/context/design_decisions.md`), QA checklist (`.ai/context/qa_checklist.md`), code review and QA run docs. Postmortem: `docs/POSTMORTEM_EPH-20250315-0001.md`. Ship: user must run `git add -A && git commit && git push origin main` in their terminal to push to GitHub.

**No further action required for this task.**
